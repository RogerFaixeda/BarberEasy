<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Apunta el estilo por defecto de todos los MaterialButton a tu PrimaryButton -->
        <item name="materialButtonStyle">@style/Widget.App.PrimaryButton</item>
        …
    </style>
    <style name="Widget.App.PrimaryButton" parent="Widget.MaterialComponents.Button">
        <!-- Aquí defines la tint en vez de android:background -->
        <item name="android:backgroundTint">@color/blue_500</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingVertical">12dp</item>
        <item name="android:paddingHorizontal">24dp</item>
    </style>

</resources>
